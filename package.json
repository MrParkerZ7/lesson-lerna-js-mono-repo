{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install": "lerna run install",
    "start": "lerna run start",
    "test": "lerna run test",
    "info:react:express": "lerna run info --scope={web-client,server-api}",
    "info:client": "lerna run info --scope=web-client",
    "new-version": "lerna version --convertional-commit --yes",
    "clean": "npx lerna clean -y",
    "boots": "npx lerna bootstrap --hoist"
  }
}